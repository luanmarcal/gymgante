# gymgante

clona
switch develop
npm install
npm install expo
npm install firebase @react-native-async-storage/async-storage
npm install firebase

*alterações*

switch -c "nome da branch"
git add .
git push

git switch develop
